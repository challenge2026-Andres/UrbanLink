# Copie este arquivo para .env e preencha os valores.
# O .env real NUNCA deve ser commitado (já está no .gitignore).

# Porta do servidor HTTP do backend
PORT=3333

# Ambiente: development | production
NODE_ENV=development

# Chave da API Olho Vivo da SPTrans (gerada em "Meus Aplicativos").
# Fica SOMENTE no backend. Nunca exponha no frontend.
SPTRANS_API_KEY=

# URL base da API da SPTrans (não precisa alterar)
SPTRANS_BASE_URL=https://api.olhovivo.sptrans.com.br/v2.1

# Origem permitida para CORS (URL do frontend Vite em dev)
CORS_ORIGIN=http://localhost:5173

# --- Validação de trajeto (Fase 3) ---

# Raio (metros) entre o celular e o ônibus mais próximo para o check-in valer.
VALIDATION_RADIUS_M=150

# Idade máxima (segundos) da captura de posição da SPTrans para ser confiável.
SPTRANS_POSITION_MAX_AGE_S=90

# Tamanho máximo (bytes) da foto enviada na validação. Padrão: 2 MiB.
MAX_PHOTO_BYTES=2097152

# APENAS DESENVOLVIMENTO: quando "true", toda validação de trajeto é aceita
# (útil para testar o fluxo no celular sem estar dentro de um ônibus).
# Ignorado automaticamente quando NODE_ENV=production.
VALIDATION_BYPASS=false

# --- Gamificação (Fase 4) ---

# Distância estimada por trajeto validado (km). Base do cálculo de CO2 e pontos.
DISTANCIA_MEDIA_KM=7

# Fatores de emissão (kg CO2 por passageiro-km). CO2 evitado = distância x (carro - ônibus).
FATOR_CO2_CARRO_KG_KM=0.171
FATOR_CO2_ONIBUS_KG_KM=0.089

# Pontos por trajeto = PONTOS_BASE + distância_km x PONTOS_POR_KM. Ecoa = pontos.
PONTOS_BASE=10
PONTOS_POR_KM=6

# Trajetos necessários para subir de nível.
TRAJETOS_POR_NIVEL=3

# Desafio semanal: meta de trajetos e recompensa em Ecoa ao concluir.
DESAFIO_SEMANAL_META=5
DESAFIO_SEMANAL_RECOMPENSA=150

# --- Análise de conteúdo da foto ---
# Verifica se a foto é mesmo o interior de um transporte (ônibus/trem/metrô).
# Roda um modelo CLIP local (@huggingface/transformers) — a foto nunca sai do servidor.

# Liga a análise (sem isso, comportamento atual: só formato/tamanho).
IMAGE_ANALYSIS_ENABLED=false

# Provedor: local (implementado) | anthropic | gemini (futuro).
IMAGE_ANALYSIS_PROVIDER=local

# Modelo transformers.js para classificação zero-shot de imagem.
IMAGE_ANALYSIS_MODEL=Xenova/clip-vit-base-patch32

# advisory = só registra/mostra o resultado; blocking = recusa foto reprovada.
IMAGE_ANALYSIS_MODE=advisory

# Soma mínima de score dos rótulos "positivos" (0..1) para aprovar a foto.
IMAGE_ANALYSIS_MIN_CONFIDENCE=0.5

# Timeout da inferência (ms).
IMAGE_ANALYSIS_TIMEOUT_MS=10000

# Em erro/timeout: open = aprova mesmo assim | closed = rejeita.
IMAGE_ANALYSIS_FAIL_MODE=open
