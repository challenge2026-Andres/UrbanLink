{
  "name": "urbanlink-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backend do UrbanLink: proxy seguro para a API Olho Vivo da SPTrans",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch --watch-path=./src --watch-path=./.env src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0",
    "zod": "^3.24.1"
  }
}
