# URL base da API do backend UrbanLink.
# Em dev o Vite já faz proxy de /api -> http://localhost:3333 (ver vite.config.ts),
# então normalmente basta "/api". Em produção, aponte para a URL pública do backend.
VITE_API_URL=/api
